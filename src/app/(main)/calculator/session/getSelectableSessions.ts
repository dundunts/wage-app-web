// import {Session} from "@/types/session.types";
//
// export function getSelectableSessions(
//     sessions: Session[]
// ) {
//     return sessions.filter((s) =>
//         ["OPENED", "OPENED_DRAFT", "RECALCULATING", "RECALCULATING_DRAFT"].includes(
//             s.status
//         )
//     );
// }
